import { EmptyState } from '../../components/common/EmptyState';

export function FeedScreen() {
  // Fase 2 implementa a listagem paginada de promoções aqui.
  return <EmptyState message="Nenhuma promoção ainda. Seja o primeiro a publicar!" />;
}
