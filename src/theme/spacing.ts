// Escala de espaçamento base 4. `xxl`/`xxxl` entraram no redesign para dar
// respiro às telas de conteúdo (feed, detalhe, perfil).
export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 40,
  xxxl: 56,
} as const;
